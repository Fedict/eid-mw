Bignum lib stripped from OpenSSL 0.9.7b
=======================================

The rsa.c, rsa.h is a small layer on top of it that allows you to
do RSA raw and RSA V1.5 operations (en/decrypt, sign/verify).

TODO: add RSA V1.5 signatures.
