#!/bin/bash

case $TRAVIS_OS_NAME in
	linux)
		sh .travis/add-trusty
		apt-get update
		apt-get -t trusty -y install automake
		apt-get install -y autotools-dev libgtk2.0-dev libpcsclite-dev libtool automake autoconf
	;;
	osx)
		continue
	;;
esac
