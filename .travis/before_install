#!/bin/bash

set -e
set -x

case $(uname -s) in
	Linux)
		add-apt-repository ppa:wouter-debian/new4trusty
		apt-get update
		apt-get install -y autotools-dev libgtk2.0-dev libpcsclite-dev libtool automake autoconf
	;;
	*)
		continue
	;;
esac
